/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'avatars.githubusercontent.com',
      '10.0.0.169',
      'localhost',
      '127.0.0.1',
    ],
  },
}

module.exports = nextConfig
